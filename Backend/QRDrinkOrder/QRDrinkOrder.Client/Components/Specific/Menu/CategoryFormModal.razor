@using QRDrinkOrder.Shared.DTOs.Requests

<div class="modal fade show d-block modal-glass" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content modal-content-premium">
            <div class="modal-header modal-header-premium">
                <h5 class="modal-title modal-title-premium">@(EditingCategoryId == 0 ? "Thêm Danh Mục Mới" : "Cập Nhật Danh Mục")</h5>
                <button type="button" class="btn-close" @onclick="OnClose"></button>
            </div>
            <div class="modal-body p-4">
                <EditForm Model="@Model" OnValidSubmit="HandleValidSubmit">
                    <DataAnnotationsValidator />
                    
                    <div class="mb-4">
                        <label class="form-label form-label-premium">Tên danh mục (Tiếng Việt) *</label>
                        <InputText class="form-control form-control-premium" @bind-Value="Model.NameVi" />
                        <ValidationMessage For="@(() => Model.NameVi)" class="text-danger small mt-1" />
                    </div>
                    <div class="mb-4">
                        <label class="form-label form-label-premium">Tên danh mục (Tiếng Anh) *</label>
                        <InputText class="form-control form-control-premium" @bind-Value="Model.NameEn" />
                        <ValidationMessage For="@(() => Model.NameEn)" class="text-danger small mt-1" />
                    </div>
                    <div class="mb-4">
                        <label class="form-label form-label-premium">Thứ tự hiển thị (Số nhỏ xếp trước)</label>
                        <InputNumber class="form-control form-control-premium" @bind-Value="Model.DisplayOrder" />
                    </div>
                    <div class="mb-4 form-check form-switch px-4 py-3 bg-light rounded-3 border">
                        <InputCheckbox class="form-check-input ms-0 me-3 mt-1" style="transform: scale(1.3);" id="catActiveCheck" @bind-Value="Model.IsActive" />
                        <label class="form-check-label fw-bold text-dark" for="catActiveCheck">Hiển thị trên Menu</label>
                    </div>
                    
                    <div class="d-flex justify-content-end gap-3 mt-5">
                        <button type="button" class="btn btn-light px-4 rounded-3 fw-bold border" @onclick="OnClose">Hủy</button>
                        <button type="submit" class="btn btn-premium px-5" disabled="@IsSaving">
                            @if (IsSaving) { <span class="spinner-border spinner-border-sm me-2"></span><span>Đang lưu...</span> }
                            else { <span>Hoàn tất</span> }
                        </button>
                    </div>
                </EditForm>
            </div>
        </div>
    </div>
</div>

@code {
    [Parameter]
    public SaveCategoryRequest Model { get; set; } = new();

    [Parameter]
    public int EditingCategoryId { get; set; } = 0;

    [Parameter]
    public bool IsSaving { get; set; }

    [Parameter]
    public EventCallback OnClose { get; set; }

    [Parameter]
    public EventCallback<SaveCategoryRequest> OnSave { get; set; }

    private async Task HandleValidSubmit()
    {
        await OnSave.InvokeAsync(Model);
    }
}
